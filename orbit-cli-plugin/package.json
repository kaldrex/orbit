{
  "name": "orbit-cli",
  "version": "0.2.0",
  "type": "module",
  "main": "index.js",
  "description": "Plumbing-only OpenClaw plugin that wraps Orbit's HTTP API + claw-side fetchers. 15 verbs covering observations, persons, meetings, topics, calendar/messages fetch, and jobs. No LLM, no classification — transport only. Observer/resolver/brief/topic SKILLs own all interpretation.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  }
}
