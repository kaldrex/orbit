# 14 · Cleanup — 2026-04-20

> Session that fixed the damage surfaced by the 2026-04-20 audit. Four phases ran. Phases A, B, C landed cleanly; Phase D is this document + supporting memory entries. **All 329 tests green. DB is consistent. Umayr card byte-identical across every change.**

## Verdict

**ALL_PHASES_PASS.**

| Phase | Status | Headline |
|---|---|---|
| A — WhatsApp depth | ✅ | 5 new rule modules, ~30 new tests, no ad-hoc scripts |
| B — Audit cleanup | ✅ | DB wiped + re-ingested from v3 with safety rules, 6,807 → 1,602 persons |
| C — Enrichment loop | ✅ | `/api/v1/persons/enriched` live; manifest-gen merges DB-enriched fields back |
| D — Docs + memory | ✅ (this doc) | stale docs refreshed, 6 debt memories saved, verification-log backfilled |

## Before / after

| metric | before | after |
|---|---|---|
| Test count | 196 | **329** (+133) |
| Persons in DB (Sanchay's user_id) | 6,809 | **1,602** (5,207 safety-filtered) |
| Phone-as-name violations | 5,028 | **0** |
| Email-as-name violations | 62 | **0** |
| Unicode-masked-phone violations | 109 | **0** |
| Test-data leaks (`apitest.lead@example.com`) | 1 | **0** |
| Duplicate Umayr person_ids | 2 | **1** (bridge-aware merger collapsed) |
| Duplicate Ramon person_ids | 2 | **1** |
| `category: "other"` rows (placeholder) | 6,807 | 0 (relationship_to_me now `null`, honest) |
| Group-name bug ("Sujay kapadnis") | 408 rows | **0** (v3 re-ingest) |
| `ORBIT_SELF_EMAIL` hardcoded | yes | **env-var driven** (multi-founder ready) |
| `resolveConfig` throws uncaught | yes | **wrapped**, returns `{error: {code:"CONFIG_MISSING"}}` |

Umayr card diff against April-19 baseline: **0 lines** across all changes. The canary held.

## Phase A — WhatsApp depth fixes

New modules in `orbit-rules-plugin/lib/`:
- `safety.mjs` — 6 drop rules from the observer SKILL as pure functions (phone-as-name, email-as-name, empty, bot names, Unicode-masked phone, forwarded-chain artifact). Used at both observer emission AND bulk transform time. Single enforcement point.
- `name.mjs` — `pickBestName()` with `messages.sender_name` fallback when `contacts.push_name` is empty. Backfilled ~500-800 previously-nameless humans.
- `group-junk.mjs` — mega-lurker (members > 200 + self outbound = 0), broadcast-ratio (top sender > 80%), commercial-keyword (regex on group name). Annotates suppressed-flag per D4 — does NOT exclude from DB.
- `bridge.mjs` (pre-existing, extended) — cross-channel fuzzy-name bridge. Consistent `bareLid()` application.
- `forwarded.mjs` (pre-existing) — vendor-on-foreign-domain stripping.

+~30 unit tests with real failing fixtures from the prior recon.

## Phase B — Audit critical cleanup

B1. `orbit-cli-plugin/index.js:70-71,99-100,118-119` — wrapped `resolveConfig()`, now returns `{error: {code: "CONFIG_MISSING", message, suggestion}}` instead of throwing. Regression test added.

B2. `scripts/manifest-to-observations.mjs` — rebuilt to read **v3 manifest** (not v2!) and apply `safety.mjs` at transform time. Dry-run validated 1,605 / 1,605 observations pass zod.

B3. `scripts/migrations/002-wipe-stage5-bulk.sql` — executed against live Supabase:
- `pg_dump` taken first (`outputs/cleanup-2026-04-20/pg-dump-pre-wipe.sql`)
- Deleted all `manifest://%` observations + derived persons
- Post-wipe counts verified exact: `persons = 2`, `observations kind='person' = 2`

B4. `scripts/generate-merges-v2.mjs` — bridge-aware. Queries existing `persons` for matching phone/email before minting new person_ids. Re-ingested 1,600 new persons; Umayr + Ramon correctly merged into their pre-existing person_ids (preserving their April-19 enriched category + relationship_to_me).

B5. `outputs/manifest-hypothesis-2026-04-19/manifest-gen.mjs` (+ claw mirror) — `sanchaythalnerkar@gmail.com` hardcode replaced with `ORBIT_SELF_EMAIL` + `ORBIT_SELF_PHONE` env vars. Script now refuses to run if env var is unset (fail-fast for multi-founder onboarding).

B6. Umayr guardrail test — fetch via `orbit_person_get`, `diff` against baseline → **byte-identical**.

## Phase C — Enrichment loop closure

C1. `supabase/migrations/20260420_select_persons_page_rpc.sql` — new `SECURITY DEFINER` RPC for cursor-paginated person lookup. Matches the existing `select_observations` + `select_person_observations` pattern so the ANON-key route can bypass RLS cleanly.

C2. `src/app/api/v1/persons/enriched/route.ts` — returns persons with non-"other" category OR non-placeholder `relationship_to_me`. Uses the new RPC (the initial direct-table version hit RLS and silently returned nothing — this was the Phase C stall).

C3. `orbit-cli-plugin` — new verb `orbit_persons_list_enriched`, paginated, 10-page circuit breaker.

C4. `outputs/manifest-hypothesis-2026-04-19/manifest-gen.mjs` — calls the enriched endpoint at start, builds `byPhone` + `byEmail` lookup index. During bucket emission: **DB wins on {category, company, title, relationship_to_me}; source wins on {last_touch, thread_count, groups, first_seen}.** Umayr's enriched prose survives re-generation.

C5. `tests/integration/manifest-gen-enrichment-loop.test.ts` (6 tests) + `tests/integration/persons-enriched-endpoint.test.ts` (10 tests, updated for the RPC mock).

Live verification: `curl /api/v1/persons/enriched?limit=2000` → exactly Umayr + Ramon, categories `team` + `founder`, `next_cursor: null`.

## Phase D — Docs + memory sync

This document (14) is the cleanup handoff. In addition:
- `agent-docs/03-current-state.md` updated (1,602 persons, 329 tests, 3 API routes live, enriched endpoint)
- `agent-docs/11-v0-pipeline-handoff-2026-04-19.md` §3.3 counts updated
- `agent-docs/README.md` index includes docs 12, 13, 14
- `outputs/verification-log.md` backfilled for Stages 4, 5, 5b, 5c
- 6 memory entries saved for the invisible-debt items flagged in audit

## Known debt (now tracked in memory)

See memory entries at `/Users/sanchay/.claude/projects/-Users-sanchay-Documents-projects-personal-orbit/memory/`:

1. Unicode-masked-phone regex → CLOSED in Phase A
2. `messages.sender_name` fallback → CLOSED in Phase A
3. CI/monitoring → still pending, tracked
4. `openclaw agent --verbose --json` observability gap → pending, tracked
5. `merged_observation_ids.min(2)` schema quirk → deferred, tracked (D5 approved)
6. Neo4j re-enablement (for scale graph traversal) → deferred, tracked

## What's preserved (audit's "do not drift" list)

- CLI error taxonomy (`errors.mjs`) — 12 codes, structured `{code, message, suggestion}`
- Pure-plumbing CLI discipline (no LLM, no `ANTHROPIC_API_KEY` in plugin)
- 5× test-to-code LOC ratio
- Cross-channel name-bridge guardrails (`bridge.mjs:102-163`)
- 80/20 deterministic/interpretive split (rules do identity, LLM does judgment)

## Next up

**Stage 6 — LLM enrichment** can now run safely. 1,600 clean skeleton cards ready for batched enrichment at ~$5 Anthropic cost. After Stage 6: Stage 7 (continuous loop) + Stage 8 (UI).

The foundation is clean. Everything downstream now builds on solid ground.
