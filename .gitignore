# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
orbit-rules-plugin/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# verification harness snapshots (kept locally only)
/.verify-runs/

# SQLite WAL / SHM sidecars — only commit the committed .db
*.db-wal
*.db-shm
.env*.local

# Supabase CLI local cache (project-ref, pooler-url, version pins)
/supabase/.temp/

# Session 2026-04-20 additions — bulky derivable artifacts
orbit-cli-plugin/node_modules
outputs/**/node_modules/
outputs/**/pre-wipe-*.ndjson
outputs/**/pg-dump-*.sql

# 2026-04-22: stripped from history (contained secrets — do NOT re-add)
openclaw-snapshot/
outputs/stage-6-v3-*/
outputs/verification/2026-04-18-track2/
tests/fixtures/session-minimal.db
tests/fixtures/wacli-minimal.db
outputs/provisioning/
